-- ============================================================
-- V2__fix_id_types.sql
-- Migration kept as a no-op for SQLite compatibility.
-- The schema is created in V1__init_setup.sql and is already
-- aligned with the integer-based JPA mappings.
-- ============================================================

SELECT 1;
